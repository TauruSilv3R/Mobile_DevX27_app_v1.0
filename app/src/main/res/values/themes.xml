<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base app theme (referenced by Manifest) -->
    <style name="Theme.DevX27" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <!-- Splash screen theme — transparent so Compose takes over immediately -->
    <style name="Theme.DevX27.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/devx27</item>
        <item name="postSplashScreenTheme">@style/Theme.DevX27</item>
    </style>
</resources>
